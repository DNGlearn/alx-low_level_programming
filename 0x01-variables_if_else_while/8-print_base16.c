#include<stdio.h>
/**
 * main - main killed best girl
 * Return: 0
 */
int main(void)
{
	int nums;
	char lets;

	nums = 0;
	lets = 'a';
	while
		(nums < 10)
		{
			putchar(nums + '0');
			nums++;
		}
	while
		(lets < 'g')
		{
			putchar(lets);
			lets++;
		}
	putchar('\n');
	return (0);
}
