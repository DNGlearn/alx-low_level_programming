#include<stdio.h>
/**
 * main - what's good main
 * Return: 0
 */
int main(void)
{
	char upper;
	char lower;

	upper = 'A';
	lower = 'a';
	while
		(lower <= 'z')
		{
			putchar(lower);
			lower++;
		}
	putchar('\n');
	while
		(upper <= 'Z')
		{
			putchar(upper);
			upper++;
		}
	return (0);
}
