#include<stdio.h>
/**
 * main - what's good main
 * Return: 0
 */
int main(void)
{
	char upper;
	char lower;

	upper = 'A';
	lower = 'a';
	while
		(lower <= 'z')
		{
			putchar(lower);
			lower++;
		}
	while
		(upper <= 'Z')
		{
			putchar(upper);
			upper++;
		}
	putchar('\n');
	return (0);
}
