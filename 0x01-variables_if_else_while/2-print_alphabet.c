#include<stdio.h>
/**
 * main - what's good main
 * Return: 0
 */
int main(void)
{
	char letter;

	letter = 'a';
	while
		(letter <= 'z')
		{
			putchar(letter);
			letter++;
		}
	putchar('\n');
	return (0);
}
